[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["python3.11 scripts/build.py"]
dependsOn = ["install"]

[start]
cmd = "uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
